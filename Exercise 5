using System.Diagnostics.CodeAnalysis;
using System.Threading;

int timer = 10;
while (timer > 0)
{
    timer --;
    Thread.Sleep(1000);
    Console.WriteLine(timer.ToString());
    
    if (timer == 0)
    {
        Console.WriteLine("Ability Ready");
    }

}
