for (int i = 1; i <= 5; i++)
{
    Console.WriteLine("Level: " + i);

    // k = i * 2, but in a loop:

    var k = 0;
    for (int j = 0; j < i; j++)
    {
        k += 2;
    }

    Console.WriteLine("enemies " + k);
}
